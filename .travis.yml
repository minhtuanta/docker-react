language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t minhtuanta/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true minhtuanta/docker-react npm run test